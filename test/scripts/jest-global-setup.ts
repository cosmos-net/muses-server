// Este archivo se ejecuta una vez antes de todas las pruebas
// Es utilizado para configuración global de pruebas
module.exports = async (): Promise<void> => {
  // Realizar cualquier configuración necesaria antes de que se ejecuten las pruebas
  console.log('Configuración global de pruebas inicializada');
};
